{
  "framework": "nextjs",
  "installCommand": "pnpm install -w --frozen-lockfile",
  "buildCommand": "pnpm -C premium-app build",
  "outputDirectory": "premium-app/.next",
  "git": {
    "deploymentEnabled": {
      "preview": true,
      "production": true
    }
  }
}
